import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
  },
  heading: {
    color: 'black',
    fontSize: 25,
    fontWeight: 'bold',
    marginLeft: 'auto',
    marginRight: 'auto',
  },
  resultContainer: {
    alignItems: 'center',
    padding: 5,
    marginTop: 50,
    justifyContent: 'center',
  },
  result: {
    fontSize: 15,
    // fontWeight: 500,
  },
});
